<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"
    android:drawable="@drawable/startipold_bright" />
    <item android:id="@android:id/progress"
        android:drawable="@drawable/startip_dark" />
    <item android:id="@android:id/secondaryProgress"
    android:drawable="@drawable/startip1_bright" />


</layer-list>

    <!--layer-list代表圖層,目前3個-->
    <!--不點時用@android:id/background-->
    <!--沒有被點擊的部份"@android:id/secondaryProgress"-->
    <!--有點按時,圖的顯示android:id="@android:id/progress"-->


